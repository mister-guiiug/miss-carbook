# Components

Ce dossier contient les composants React réutilisables.
- Sous-dossiers par fonctionnalité (ex: `ui/`, `assistant/`, `workspace/`)
- Composants nommés en PascalCase
- Export par index barrel quand nécessaire