{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Miss Carbook: Chrome (serveur déjà lancé)",
      "type": "chrome",
      "request": "launch",
      "url": "http://localhost:5173",
      "webRoot": "${workspaceFolder}"
    },
    {
      "name": "Miss Carbook: npm run dev + Chrome",
      "type": "chrome",
      "request": "launch",
      "url": "http://localhost:5173",
      "webRoot": "${workspaceFolder}",
      "preLaunchTask": "vite: dev"
    }
  ]
}
