# Supabase
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key-here

# Sentry (Error Tracking)
# `VITE_SENTRY_ENVIRONMENT` a été retiré : rien ne le lisait. `main.tsx` passe
# `environment: import.meta.env.MODE` à `initSentry`, que Vite renseigne seul.
VITE_SENTRY_DSN=https://0445a1e0a5fcc5319a0584a960c48e06@o4511240922005504.ingest.de.sentry.io/4511242741809232

# Mesure d'audience — clé de projet PostHog, nuage EUROPÉEN (ADR 0012). La MÊME
# pour tout le parc : un seul projet, les applications distinguées dedans par
# `app_name`, que le socle déduit du chemin de base. Publique par conception.
VITE_POSTHOG_KEY=phc_XXXXXXXXXXXXXXXXXXXXXXXXX

# Build
VITE_BASE_PATH=/
NODE_ENV=production
